name: Firebase App Distribution CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-distribute:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Java
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 3. Setup Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 4. Accept licenses
      - name: Accept Android Licenses
        run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses

      # 5. Install Build Tools (Fix for zipalign error)
      - name: Install Android Build Tools
        run: |
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "build-tools;36.0.0"

      # 6. Make gradlew executable
      - name: Grant permission
        run: chmod +x gradlew

      # 7. Build Release APK
      - name: Build Release APK
        run: |
          ./gradlew assembleRelease \
            -PVERSION_CODE=${{ github.run_number }} \
            -PVERSION_NAME=1.0.${{ github.run_number }}

      # 8. Sign APK
      - name: Sign Release APK
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.ANDROID_KEYSTORE }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          alias: ${{ secrets.ANDROID_DEVS_ALIAS }}
          keyPassword: ${{ secrets.ANDROID_DEVS_ALIAS_PASSWORD }}
          buildToolsVersion: 36.0.0

      # 9. Install Firebase CLI
      - name: Install Firebase Tools
        run: npm install -g firebase-tools

      # 10. Authenticate Firebase
      - name: Authenticate Firebase
        run: echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > service-account.json

      # 11. Upload to Firebase
      - name: Upload to Firebase
        run: |
          # Specifically find the signed output file
          APK_PATH=$(find app/build/outputs/apk/release -type f ! -name "*unsigned*" -name "*.apk" | head -n 1)
          
          echo "Uploading APK found at: $APK_PATH"

          export GOOGLE_APPLICATION_CREDENTIALS=service-account.json

          firebase appdistribution:distribute "$APK_PATH" \
            --app 1:439662827938:android:41cef8ff572e6aa3637afb \
            --groups "internal-testers" \
            --release-notes "Automated build from GitHub Actions (Run #${{ github.run_number }})"